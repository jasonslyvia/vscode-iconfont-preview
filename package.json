{
    "name": "vscode-iconfont-preview",
    "displayName": "Iconfont Previewer",
    "description": "👓 preview your iconfont icon on the gutter",
    "version": "0.1.0",
    "publisher": "undefinedvs",
    "engines": {
        "vscode": "^1.20.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onLanguage:scss",
        "onLanguage:css"
    ],
    "main": "./out/extension",
    "icon": "icon.png",
    "contributes": {
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "@types/mocha": "^2.2.42",
        "@types/node": "^9.4.7",
        "tslint": "^5.8.0",
        "typescript": "^2.6.1",
        "vscode": "^1.1.6"
    },
    "dependencies": {
        "node-fetch": "^2.1.1"
    }
}
